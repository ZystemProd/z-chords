{
  "name": "z-chords-tests",
  "private": true,
  "type": "module",
  "description": "Browser tests for Z-Chords. Kept out of the app so the app stays dependency-free.",
  "scripts": {
    "test": "node run.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^23.0.0"
  }
}
